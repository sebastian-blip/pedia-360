---
import '../styles/variables.css';

const benefits = [
  "Saber cuándo actuar en casa y cuándo acudir a urgencias",
  "Entender mejor el comportamiento, sueño y alimentación de tu hijo según su etapa",
  "Tener claridad en momentos donde normalmente sentirías miedo o ansiedad",
  "Aprender a manejar enfermedades comunes infantiles desde casa de forma segura",
  "Crear rutinas saludables sin caer en extremos ni culpa",
  "Resolver dudas reales con acompañamiento profesional",
  "Sentirte más segura tomando decisiones como mamá"
];
---

<section class="benefits">
  <div class="benefits-container">
    <h2>Lo que vas a lograr dentro de la membresía</h2>

    <div class="benefits-grid">
      {benefits.map((benefit, index) => (
        <div
          class="benefit-item"
          style={`
            --benefit-bg: ${
              index % 2 === 0
                ? 'linear-gradient(135deg, var(--color-light-purple), var(--color-yellow))'
                : 'linear-gradient(135deg, var(--color-light-pink), var(--color-yellow))'
            };
          `}
        >
          <p>{benefit}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .benefits {
    padding: var(--space-2xl) var(--space-lg);
    background: var(--color-white);
  }

  .benefits-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  h2 {
    text-align: center;
    color: var(--color-dark);
    margin-bottom: var(--space-2xl);
    font-size: var(--text-3xl);
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-lg);
  }

  .benefit-item {
    background: var(--benefit-bg);
    padding: var(--space-lg);
    border-radius: 16px;
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
  }

  .benefit-item:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .benefit-item p {
    margin: 0;
    color: var(--color-dark);
    font-weight: 600;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .benefits {
      padding: var(--space-xl) var(--space-md);
    }

    h2 {
      font-size: var(--text-2xl);
      margin-bottom: var(--space-xl);
    }

    .benefits-grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }
  }
</style>